import React from "react";
import Conferences from "@/components/Conferences/Conferences";

type Props = {};

const conferences = (props: Props) => {
  return <Conferences />;
};

export default conferences;
